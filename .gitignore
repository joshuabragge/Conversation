# Xcode project is generated by XcodeGen from project.yml — regenerate
# with `xcodegen generate` rather than committing it, so it can't drift
# out of sync with project.yml or produce merge conflicts.
Conversation.xcodeproj/

# Swift Package Manager
.swiftpm/
.build/

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# macOS
.DS_Store
