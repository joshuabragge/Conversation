import XCTest

final class ConversationTests: XCTestCase {
    /// M0 smoke test — replaced by real VADSegmenter/LanguageIdentifier
    /// unit tests as those modules land in later milestones.
    func testScaffoldSanity() throws {
        XCTAssertTrue(true)
    }
}
